const initialStateTracks = [
  'Like me', 'New Orlean Song', 'Sandman'
];

export default (state = initialStateTracks, action) => {
  if(action.type === 'ADD_TRACK') {
    return [
      ...state, action.payload
    ];
  }
  return state
}