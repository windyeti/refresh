const initialState = [
  ['Like me', 'Sandman'],
  ['Like me', 'New Orlean Song', 'Sandman'],
  ['Sandman', 'New Orlean Song']
];

export default (state = initialState, action) => {
  if(action.type === 'ADD_PLAYLIST') {
    return [
      ...state, action.payload
    ]
  }
  return state
}